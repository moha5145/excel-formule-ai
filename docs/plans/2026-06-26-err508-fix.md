# Plan d'implémentation — Correction Err:508 LibreOffice Calc

**Fichier modifié** : `src/lib/excelExport.ts` (2 changements)

---

### Tâche 1 : Forcer le recalcul complet à l'ouverture

- [ ] **Étape 1 : Ajouter `fullCalcOnLoad` après la création du workbook**

Dans `src/lib/excelExport.ts`, ligne ~454, après `workbook.modified = new Date();` :

```typescript
workbook.calcProperties.fullCalcOnLoad = true;
```

**Avant (lignes 451-455)** :
```typescript
const workbook = new ExcelJS.Workbook();
workbook.creator = "Excel-Compta AI";
workbook.created = new Date();
workbook.modified = new Date();
```

**Après** :
```typescript
const workbook = new ExcelJS.Workbook();
workbook.creator = "Excel-Compta AI";
workbook.created = new Date();
workbook.modified = new Date();
workbook.calcProperties.fullCalcOnLoad = true;
```

---

### Tâche 2 : Ajouter un résultat par défaut à la formule active

- [ ] **Étape 2 : Remplacer `{ formula: formulaFR }` par `{ formula: formulaFR, result: 0 }`**

Dans `src/lib/excelExport.ts`, ligne ~792 :

```typescript
resCell.value = { formula: formulaFR, result: 0 };
```

**Avant** :
```typescript
resCell.value = { formula: formulaFR };
```

**Après** :
```typescript
resCell.value = { formula: formulaFR, result: 0 };
```

---

### Tâche 3 : Vérification

- [ ] **Étape 3 : Lancer le build et vérifier qu'il n'y a pas d'erreur**

```bash
npm run build
```

Attendu : le build réussit sans erreur TypeScript ni ESLint.

- [ ] **Étape 4 : Test manuel**
  1. Ouvrir l'application dans le navigateur
  2. Générer une formule via l'IA
  3. Télécharger le fichier .xlsx
  4. Ouvrir le fichier dans **LibreOffice Calc**
  5. Vérifier que la cellule verte (résultat) affiche le résultat correct, pas `Err:508`

- [ ] **Étape 5 : Commit**

```bash
git add src/lib/excelExport.ts
git commit -m "fix: force full recalculation on load and provide default cached result to prevent Err:508 in LibreOffice Calc"
```
